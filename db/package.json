{
  "name": "db",
  "version": "1.0.0",
  "description": "",
  "main": "utils.js",
  "scripts": {
    "migrate": "BLUEBIRD_WARNINGS=0 sequelize db:migrate",
    "migrate:undo": "BLUEBIRD_WARNINGS=0 sequelize db:migrate:undo",
    "migrate:create": "BLUEBIRD_WARNINGS=0 sequelize migration:create",
    "seed": "BLUEBIRD_WARNINGS=0 sequelize db:seed:all",
    "seed:undo": "BLUEBIRD_WARNINGS=0 sequelize db:seed:undo:all",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "den <test@test.com>",
  "license": "ISC",
  "dependencies": {
    "config": "^1.30.0",
    "pg": "^7.4.3",
    "pg-hstore": "^2.3.2",
    "sequelize": "^4.37.7",
    "sequelize-cli": "^4.0.0"
  }
}
